spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

server:
  port: 8083

eureka:
  client:
    service-url:
      defoultZone: http://localhost:8082/eureka-server